# Copy this to .env, fill in real values, then: chmod 600 .env
# .env is loaded by the systemd unit via EnvironmentFile= -- see
# pi-mcp-server.service.

MCP_BASE_URL=https://mcp.pre-idea.com
MCP_OWNER_EMAIL=your-gmail-address@gmail.com

GOOGLE_CLIENT_ID=your-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-client-secret

PIXOO_IP=10.0.0.212

# Must match PIXOO_PAUSE_FLAG_PATH in mbta-display's own .env (check
# there before deploying -- this default only matches if it hasn't been
# overridden).
PIXOO_PAUSE_FLAG_PATH=/tmp/pixoo_pause.flag

MCP_AUDIT_LOG=/home/abbykatz/pi-mcp-server/activity.log

# Second Pi, checked read-only over SSH (password login -- sshpass must
# be installed: sudo apt install sshpass). REMOTE_PI_SERVICES is a
# comma-separated allow-list of systemd unit names on that Pi; nothing
# outside this list is reachable. Find unit names by SSHing in yourself
# and running: systemctl list-units --type=service --state=running
REMOTE_PI_HOST=10.0.0.236
REMOTE_PI_USER=oli
REMOTE_PI_PASSWORD=change-me
REMOTE_PI_SERVICES=oli-web
